# Jellify Desktop — Per-Screen Design Spec

**Status:** Source-of-truth spec for the SwiftUI desktop client.
**Design reference:** `design/project/` prototype (1440×900 locked stage, Figtree, Purple/Ocean/Forest/Sunset/Peanut presets, dark/OLED/light modes).
**Design direction from user feedback (`chats/chat1.md`):** no gradient washes behind album hero — replaced with editorial type + liner-note stats + dotted-pattern signature. Keep breadcrumbs for "place tracking." Avoid generic AI-app aesthetics.
**Tokens (the only colors you may introduce):** see `design/project/assets/jellify.css` — `--jf-purple-deep #0C0622`, `--jf-pink-accent #CC2F71`, `--jf-amethyst rgba(126,114,175,1)`, `--jf-teal #57E9C9`, `--jf-primary-dark #887BFF`, `--jf-danger-dark #FF066F`. Type scale: 10/12/14/16/20/24/28/34/48 (add 42, 56, 72 already used in Home + Album + FullPlayer). Radii: 4 / 8 / 12 / 16 / 24 / 999.
**Data layer:** Rust core (`core/src/client.rs`, `core/src/player.rs`, `core/src/storage.rs`) exposes `JellyfinClient.{artists, albums, album_tracks, search, stream_bytes, stream_url, image_url, authenticate_by_name}`. UniFFI bindings live in `macos/Sources/JellifyCore/Generated`. SwiftUI screens bind via `@Observable` models in `macos/Sources/Jellify/AppModel.swift`.
**Platform conventions:** macOS first (traffic lights, ⌘K, ⌘,, menubar, drag-window title bar). The design prototype draws custom traffic lights but a real SwiftUI `WindowStyle(.hiddenTitleBar)` will suffice.

---

## 1. Login

**Brief.** The front door. Must feel confident and editorial, not like a sign-in dialog. Center a single column at ~420px wide on a dark purple field. Lead with the jellyfish mark, set the word "Jellify" in italic 800, drop into a server URL field, then username / password. No photos, no gradients-behind-glass; use the same dotted-pattern signature the album hero uses for texture. Validate the server inline (ping `/System/Info/Public`) so the user sees "Jellyfin 10.9.11 · Home" before they ever type a password.

### Issue 1: Login screen chrome + layout
**Labels:** `area:macos`, `area:design`, `kind:feat`, `priority:p0`
**Effort:** M

- Full-window `Color(hex: 0x0C0622)` background, traffic lights on a draggable title bar, no sidebar, no player bar.
- Center column 420×auto. Brand lockup (30pt jellyfish + "Jellify" italic 800 at 40pt + "DESKTOP" 10pt uppercase letter-spacing 0.08em) top-anchored 120pt from chrome.
- Dotted-pattern signature (radial-gradient dots 8px×8px, `ink3` at 0.35 opacity) top-right 120×60.
- Three stacked fields: Server URL, Username, Password. Each = pill radius 12, background `surface`, border `border`, 14pt Figtree Medium, 44pt tall.
- Primary "Sign in" button: accent (`#CC2F71`) pill, 44pt tall, full width of column, shadow `0 10px 24px accent55`.
- Acceptance: tabbing moves through fields in order; Enter submits; empty validation states show a 12pt `danger` hint under the field.

### Issue 2: Inline server URL validation
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Debounced (400ms) ping to `JellyfinClient.public_info()` after URL looks valid (contains scheme + host).
- While pinging: 12pt `teal` "Checking…" with a 10pt spinner.
- On success: 12pt `teal` "Jellyfin 10.9.11 · {ServerName}" with a tiny `check-circle` icon.
- On failure: 12pt `danger` "Can't reach that server." with a `Retry` link.
- Stash the last 3 validated servers in a secondary "Recent servers" chip row below the URL field.

### Issue 3: Quick-connect + device name
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** S

- "Use Jellyfin Quick Connect" secondary link under primary button — opens a modal showing a 6-digit code + QR generated by `JellyfinClient.quick_connect_initiate` (add to core).
- Device name auto-populates from host (e.g. "Soren's MacBook Pro"); editable via a gear popover.

### Issue 4: Login error + offline states
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- 401 → shake animation on password field + "Wrong username or password."
- Network offline → banner above the column: "No network. Check your connection."
- Server unreachable twice in a row → "Last connected 3 days ago. View cached library →" link that opens Library in offline mode using SQLite cache.

---

## 2. Home

**Brief.** Time-aware greeting, "in rotation" feel. Leads with an italic 42pt h1 ("good morning, soren") and a one-liner that name-drops the server — the prototype already does this well. Keep it a content river: three QuickTiles as a row, then carousels (Recently Played, Artists You Love, Jump Back In, Your Playlists, Recently Added) at 12pt column gap, `170px minmax` grid. No hero imagery — the content IS the hero.

### Issue 5: Home greeting header + CTAs
**Labels:** `area:macos`, `area:design`, `kind:feat`, `priority:p0`
**Effort:** M

- Eyebrow "IN ROTATION" 12pt `ink2` letter-spacing 0.1em.
- h1 italic 42pt 900, time-branched copy: `<5h → "still up?"`, `<12 → "good morning, {firstName}"`, `<18 → "good afternoon"`, else "good evening".
- Body 14pt `ink2` sentence with inline `accent` italicized "instant mix" word (link).
- Right-aligned two BigBtns: "Instant Mix" (primary = ink background) + "Shuffle All" (ghost border).
- 4,208-tracks copy pulls from `JellyfinClient.user_stats` (add to core if missing).

### Issue 6: Quick tiles row (3-column)
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- 3-col grid, 10pt gap, 60pt row height. `QuickTile`: 48pt artwork + title + play-on-hover floating circle (primary bg, `0 6px 14px primary/40` shadow, slides in from right).
- Content ranking: top 3 `recently_played` or `most_played` from last 7 days, whichever yields more distinct items. Fallback to the first 3 pinned playlists.

### Issue 7: Recently Played carousel
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- `Carousel` wrapper with 18pt `SectionHeader` + "See all" trailing.
- Grid `repeat(auto-fill, minmax(170px, 1fr))`, 12pt gap, shows 8 items.
- Uses `AlbumCard`: square art, play-on-hover overlay, bottom-right circle button, title 13pt/700 + `{artist} · {year}` 11pt `ink2`.
- Data: `core.recently_played(limit: 8)` — needs new Rust method wrapping Jellyfin's `/Items/Latest` with sorting.

### Issue 8: Artists You Love row
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Section header + 6-col grid of circle artist cards (borrow `AlbumCard` with `circle` prop, radius 999).
- Name 13pt/700, genre/frequency 11pt `ink2`.
- Data: compute from play counts + favorite count. `core.top_artists(limit: 6, window: "90d")`.

### Issue 9: Jump Back In carousel
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Same shape as Recently Played, but source = partially-played items (resume-capable tracks, playlists with incomplete progress).
- Data: `core.resumable_items(limit: 8)` — Jellyfin returns `UserData.PlaybackPositionTicks > 0`.

### Issue 10: Your Playlists carousel
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Pulls from `core.user_playlists(sort: .dateModified, limit: 8)`. Shows playlist tiles, count-of-tracks in subtitle.
- Clicking navigates to Playlist detail (issue 28–34).

### Issue 11: Recently Added carousel
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- `core.recently_added(limit: 8)` wrapping Jellyfin `/Items/Latest?IncludeItemTypes=MusicAlbum&SortBy=DateCreated`.
- Mini `NEW` label (10pt uppercase accent) on the top-right of the artwork for items created in last 7 days.

### Issue 12: Home empty + skeleton states
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- Skeletons: artwork tiles = shimmering `surface` 170×170 rounded 8pt, text rows = 12pt pill placeholders.
- Empty (no plays yet): swap the greeting line to "You're new here — let's find something." and replace carousels with a single "Import your library" card + "Pick a playlist to try" row of curated seeds.

---

## 3. Library

**Brief.** The archivist's view. Pill-style chip tabs (Tracks / Albums / Artists / Playlists / Downloaded), a list↔grid toggle locked to an 8pt pill with an `ink`-backed selected state, and a filter/sort row. Defaults: Tracks → list, Albums → grid, Artists → grid-circle, Playlists → grid-square, Downloaded → list. Surface an A–Z index rail on the right for long lists.

### Issue 13: Library tab chips + count subline
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Tabs: Tracks, Albums, Artists, Playlists, Downloaded — using existing `Chip` component (7pt/14pt, `ink` active bg).
- Subline 11pt `ink3` uppercase: "{n} tracks · Sorted by {sort}" updates live.

### Issue 14: List / Grid view toggle
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- 2-segment control using existing `ViewToggle`: list icon / grid icon in a 3pt padded `surface` pill with `border`.
- Per-tab defaults remembered in `@AppStorage("library.view.tracks")` etc.

### Issue 15: Filter popover
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** M

- `IconBtn` for filter opens a 280pt popover anchored to the icon.
- Groups: Genre (checklist), Year (double-thumb range slider), Only downloaded (toggle), Only favorited (toggle), Format (FLAC / ALAC / MP3 checkboxes), Duration (< 3m / 3–6m / > 6m).
- "Clear all" + "Apply" footer. Active filter count shows on the filter icon as a 10pt pink dot.

### Issue 16: Sort menu
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Sort options: A–Z, Z–A, Recently Added, Recently Played, Most Played, Longest, Shortest, Year ↑/↓.
- Native `Menu` (SwiftUI) rendered with our styling — 12pt font, check mark on active.

### Issue 17: A–Z scroll index
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** M

- When tab is Tracks/Albums/Artists and count > 200, show a vertical `A B C … 0–9` rail 20pt wide on the far right of the content column.
- Click → scrolls to first item starting with that letter. Active letter hi-lit with `accent`.

### Issue 18: Track list — density + multiselect
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** M

- Row height: 48pt roomy / 36pt compact (bound to Tweaks sidebar density).
- Cmd-click to multiselect, Shift-click for range, Esc to clear. Selection banner slides in at bottom of content: "{n} selected — Play / Queue / Favorite / Add to playlist / Remove".

### Issue 19: Empty library state per tab
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- Tracks empty: "No tracks yet. Add music to your Jellyfin server to get started." + `Open server docs` link.
- Downloaded empty: "Nothing offline yet. Right-click any album and choose Download."
- Illustration uses the jellyfish mark tinted `amethyst/40` at 80pt.

---

## 4. Album detail

**Brief.** The editorial centerpiece. Honor the user's feedback — no gradient wash. Left: 240pt square artwork. Right: eyebrow `LONG-PLAYER · {year}`, title italic 72pt 900, "by {artist}" underlined accent, then a four-stat liner-note strip (Tracks / Minutes / Label / Format). Transport row below the hero separator: play (54pt accent circle), shuffle, heart, download, add-to-playlist, overflow. Then track list with per-track hearts + downloaded check.

### Issue 20: Album hero polish — stat strip typography
**Labels:** `area:macos`, `area:design`, `kind:polish`, `priority:p0`
**Effort:** S

- Stats: tabular-nums 22pt/800 on `ink`, uppercase 10pt label `ink3`, 28pt gap between stats. Exactly matches `Stat` component in `screens.jsx:364`.
- Four stats: Tracks, Minutes, Label (e.g. "Kranky"), Format ("FLAC 16/44.1"). If multi-format, show "FLAC · MP3" dual.

### Issue 21: Format badge with bitrate tooltip
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Hovering the Format stat reveals a popover: codec + sample rate + avg bitrate + total size.
- Pulls from `track.media_streams[0]` in Jellyfin `Item` response.

### Issue 22: Liner-notes drawer
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** M

- "Liner Notes" button in the hero right column opens a right-side drawer overlaying the main scroll area (not the right panel).
- Shows full credits, recording notes, review excerpts, release notes — sourced from `Item.Overview` + custom `NFO` tags where present.
- Close on Esc or backdrop click.

### Issue 23: Favorite / Download / Add-to-playlist actions
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Heart toggles favorite via `JellyfinClient.set_favorite(item_id, true)` (add to core).
- Download kicks off a `core.download(item_id)` job, shows a 10pt `primary` progress ring around the icon, then swaps to a `check-circle` on completion.
- Plus icon opens an "Add to playlist" popover with existing playlists list + "New playlist" at top.

### Issue 24: Related albums rail
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Below track list: `SectionHeader` "More from {artist}" + a `Carousel` of 6 albums by the same artist (fallback to same genre).
- Data: `core.albums_by_artist(artist_id, exclude: current_album_id, limit: 6)`.

### Issue 25: Appears on / features rail
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** S

- If tracks on this album feature guest artists, show "Guests on {album}" row with artist circles.
- Data: scan `track.artist_items` for non-primary artists.

### Issue 26: Disc divider for multi-disc albums
**Labels:** `area:macos`, `kind:polish`, `priority:p2`
**Effort:** S

- When tracks span multiple discs: insert a 10pt uppercase `ink3` `DISC 1` / `DISC 2` row between track groups.
- Click the disc label to play from that disc.

### Issue 27: Release date + runtime footer
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- Already present in prototype. Formalize: "Released {fullDate} · {label} · {totalRuntime}". `formatTime()` handles hh:mm:ss.

---

## 5. Artist detail

**Brief.** The pilgrimage page. Full-width 280pt hero band with a blurred backdrop from the artist's portrait (not a gradient; use a real image at 40% opacity behind a `scrim-92` wash), artist photo 200×200 (circle), name italic 72pt, genre line, stats strip. Tabs below: Discography (grouped by type: Albums / EPs / Singles / Live / Compilations), Top Tracks, About, Similar Artists, Playlists featuring this artist. Sticky transport (play all / shuffle / follow / radio).

### Issue 28: Artist hero band
**Labels:** `area:macos`, `area:design`, `kind:feat`, `priority:p0`
**Effort:** L

- 280pt band, horizontal layout: 200pt circle portrait left, type block right.
- Backdrop: if `PrimaryImageTag` exists, use `image_url(id, tag, maxWidth: 1600)` at 0.35 opacity with a 40pt blur; otherwise keep solid `bg` + dotted pattern.
- Eyebrow "ARTIST". Title italic 72pt 900. "{genres.joined(", ")}" 16pt `ink2`. Stats strip: Albums, Tracks, Followers (if Jellyfin plugin exposes it), Monthly Listeners (fallback to "plays last 30d").

### Issue 29: Play all / Shuffle / Follow / Radio transport
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Same row as album transport: 54pt accent Play, then Shuffle, heart-outline (Follow), mix-icon (Instant Mix on this artist), radio-icon (Start Artist Radio), dots-v.
- "Follow" tied to Jellyfin's `UserData.IsFavorite` on the artist item.

### Issue 30: Top Tracks (compact list, 5 rows)
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- 5-row `TrackRow` list, no Album column (reuse `showAlbum={false}`).
- Each row shows plays on the right. "See all" opens a full tracks modal.
- Data: `core.top_tracks_for_artist(artist_id, limit: 5)`.

### Issue 31: Discography grouped by type
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Sections: Albums, EPs, Singles, Live, Compilations — determined by Jellyfin `ProductionType` or inferred from track counts + `AlbumType` tag.
- Each is a `Carousel` of `AlbumCard`s, newest first.
- "See all" opens a filtered Library view pre-scoped to artist.

### Issue 32: About / bio
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Expandable block (3-line clamp at 14pt body, "Read more" reveals the rest).
- Pulls from `Item.Overview`. Secondary: Born, Origin, Active Years — from provider metadata.
- Provider logo footer ("via MusicBrainz") if source known.

### Issue 33: Similar Artists row
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- 6 circle artist cards. Data: `core.similar_artists(artist_id, limit: 6)` — wrap Jellyfin's `/Artists/{id}/Similar`.
- Click → Artist detail.

### Issue 34: Playlists featuring artist
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** S

- Horizontal playlist carousel where the artist appears in the track list.
- Data: `core.playlists_containing_artist(artist_id, limit: 6)` — iterate user playlists.

---

## 6. Playlist detail

**Brief.** Jellyfin playlists feel alive when editable. Hero: 4-up collage artwork from first four tracks (or a custom cover), editable title (Figtree italic 60pt, click-to-edit), editable description (14pt `ink2`, expandable). Transport row below. Track list with drag handles. Smart-playlist rules live at the bottom behind a "Rules" disclosure.

### Issue 35: Playlist hero with click-to-edit title + description
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Same 240×240 artwork slot as Album, but use a 2×2 collage of the first four tracks' art (SwiftUI `Grid`). User can override with a custom cover (drop-zone in the slot, overlay on hover).
- Title: click-to-edit inline text field; autosaves on blur via `JellyfinClient.update_playlist`.
- Description: multi-line inline editor, 14pt `ink2`, placeholder "Add a description…".

### Issue 36: Drag-reorder tracks
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- 24pt `grip` icon on hover, leftmost column before the row number.
- SwiftUI `.onMove` to reorder. Commits via `JellyfinClient.reorder_playlist_items`.
- Optimistic UI with rollback on server error.

### Issue 37: Add / remove tracks
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- "Add tracks" button at the top of the list → opens a modal sheet with the library browser pre-filtered to Tracks.
- Remove via context menu or keyboard `Delete` on selected rows.

### Issue 38: Share / export playlist
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** M

- Overflow menu: "Share" (copy Jellyfin deep link), "Export as M3U" (save dialog), "Export as JSON" (for backup).
- Uses native `NSSavePanel`.

### Issue 39: Smart playlist rules editor
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** L

- Only renders if playlist is smart (Jellyfin `Item.Type == "Playlist"` + `SmartFilters` property present — otherwise we'll need to proxy smart-rules metadata in our own core storage).
- Disclosure panel "Rules": each rule = field dropdown (Artist / Year / Genre / Rating / Play count) + operator + value. "Match all" / "Match any" toggle. Limit + sort row.
- Save → `core.save_smart_playlist_rules(playlist_id, rules)`.

### Issue 40: Collaborative / public indicators
**Labels:** `area:macos`, `kind:polish`, `priority:p2`
**Effort:** S

- If Jellyfin marks the playlist as shared, show a `users` icon + "Shared with 3 people" under the title.
- Owner badge next to contributed tracks.

---

## 7. Search

**Brief.** Heavy lifter. 18pt-type search field in a 14pt-radius pill. Instant-as-you-type (250ms debounce, 2-char min). Results group into Top Result (hero card), Artists, Albums, Tracks, Playlists, Genres — each an independent `Section` with "See all" link. Scope chips at top (All / Artists / Albums / Tracks / Playlists / Podcasts). Recent searches as history chips when the field is empty.

### Issue 41: Search input + clear + ⌘K keybinding
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Already in prototype. Formalize: autofocus on screen mount, Esc clears text, Esc-twice returns to previous screen.
- ⌘K or ⌘F from anywhere in the app jumps to Search + focuses the field.

### Issue 42: Instant-search with debounced fetch
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- 250ms debounce. Show a 10pt `teal` "Searching…" caption under the field.
- `JellyfinClient.search(query)` returns `SearchResults { artists, albums, tracks, playlists }`.
- Cancel previous in-flight request on each new keystroke (Swift `Task` cancellation).

### Issue 43: Scope chips
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Below the input: chip row "All / Artists / Albums / Tracks / Playlists". Active = `ink`-filled.
- Scoping shrinks the result set to that category's section + expands it to fill.

### Issue 44: Top Result hero card
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Already designed. Ranking: if the query is a prefix of exactly one artist name → pick that; else most-played match; else the first returned artist.
- Play button on the hero card (52pt accent circle).

### Issue 45: Results sections layout
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Artists → horizontal row of 6 circles.
- Albums → `AlbumGrid cols={5}`.
- Tracks → `TrackListHeader` + up to 8 `TrackRow`s.
- Playlists → `AlbumGrid cols={5}` (square).
- Genres → `GenreTile` row (exactly matching `screens.jsx:375`).

### Issue 46: No-results state
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- Already in prototype. Add a suggestion line: "Try: artist name only, or an album title." + "Search Jellyfin metadata" secondary button (explicit remote search via `core.server_search`).

### Issue 47: Recent searches with per-item clear
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Stored in `@AppStorage` as last 10 queries.
- Each row: `history` icon + query + `close` (removes from history).
- Footer: "Clear history".

### Issue 48: Genre browse tiles
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** S

- Already designed: gradient `GenreTile`s. Data: `core.genres()` sorted by track count desc, top 12.
- Click → genre-scoped results.

---

## 8. Discover

**Brief.** Recommendation-heavy sibling of Home. Same page structure, different data sources. Lead with "Discover" 34pt h1 + "Curated from what you play" subline. Sections: Instant Mix, For You (recommendation engine), Genres to Explore, Year Browse (decade tiles: 60s/70s/80s/90s/00s/10s/20s), Recently Discovered Artists (artists you favorited in the last 30d).

### Issue 49: Discover header + Instant Mix CTA
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Title italic 34pt. Subline 14pt `ink2`. Right-aligned "Start Instant Mix" primary button + "Generate new mix" ghost.

### Issue 50: For You recommendations carousel
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- 8 album/artist cards based on listening history.
- Data: `core.recommendations(limit: 8)` — wrap Jellyfin's recommendations endpoint if available, otherwise a simple algorithm: artists similar to top-3 played, minus already-played.

### Issue 51: Genres to Explore tiles
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- 4×2 grid of `GenreTile`s (bigger, 120pt tall).
- Rank = genres least-played but present in library.

### Issue 52: Decade browse row
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** S

- Horizontal tile row: '60s, '70s, '80s, '90s, '00s, '10s, '20s with the default artwork gradient + huge italic decade text.
- Click → Library pre-filtered to year range.

### Issue 53: Recently Discovered Artists
**Labels:** `area:macos`, `kind:polish`, `priority:p2`
**Effort:** S

- Artists favorited or first-played in last 30d, 6 circles.

---

## 9. Radio

**Brief.** Stations, not tracks. Lead with a horizontal row of user's "pinned" stations (persistent radio presets — a Jellify concept, stored locally). Below: Station types (Artist Radio, Song Radio, Genre Radio, Decade Radio, Mood Radio) each a 4-tile row. Running a station replaces the queue and marks `isRadio: true` so Up Next keeps auto-generating.

### Issue 54: Pinned stations row
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- User pins stations (e.g., "Grateful Dead Radio", "Jazz Drive Radio"). Stored in MMKV-equivalent (`JellifyStorage.pinned_stations`).
- Tile shows an `on air` dot, station name italic 20pt, subtitle "42 tracks · updated today".
- "Add station" pill at the end of the row.

### Issue 55: Artist Radio tile row
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- 4 circle tiles for top artists. Click starts radio seeded on that artist via `core.radio_for_artist(artist_id)`.

### Issue 56: Song Radio (from current context)
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- "Song Radio" tile always available, labeled with the currently-playing track. Click → starts radio from that song.
- Available from track context menu too.

### Issue 57: Genre / Decade / Mood radio rows
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Three rows, each 4–6 tiles.
- Moods sourced from tags (if present): chill, focus, workout, sleep, party.

### Issue 58: Radio in-progress UI (queue label)
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- When radio is active, the Queue header changes from "Up Next · From {album}" to "Radio · Seed: {seed}" with an on-air dot.

---

## 10. Settings (and Preferences window)

**Brief.** Native macOS Preferences style (⌘, opens it in its own window), but we also embed a matching Settings page inside the main window for users who want everything in one place. 780pt wide, split left-nav at 180pt. Sections: Account, Playback, Library, Appearance, Shortcuts, Notifications, Advanced, About.

### Issue 59: Preferences window shell (⌘,)
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Separate `Window("Preferences")` with `.windowStyle(.titleBar)` + `.windowResizability(.contentSize)`.
- Left nav (180pt) with 8 items (icons + labels). Right content pane scrollable.
- ⌘, binding in the app menu.

### Issue 60: Account section
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Avatar (gradient circle, current server initials), username, server URL + "Connected" status.
- Buttons: "Switch server", "Log out", "Manage account" (opens server URL + user profile in browser).

### Issue 61: Playback section — streaming + download quality
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Streaming quality: Automatic / Low (128k MP3) / Normal (320k MP3) / Lossless (FLAC) / Original (passthrough). Segmented control.
- Download quality: same options.
- Tied to Jellyfin profile/transcoding options sent in stream requests.

### Issue 62: Playback — crossfade + gapless + replay gain + normalization
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** M

- Crossfade: slider 0–12s (Apple Music matches this).
- Gapless: toggle (default on). Only applicable where source files are gapless-tagged.
- Replay Gain: Off / Track / Album segmented.
- Volume Normalization: toggle + target dB slider.

### Issue 63: Playback — output device picker
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** M

- Lists Core Audio devices via `AVAudioSession`. Persists selection + falls back gracefully.
- "Exclusive mode" toggle for lossless output.

### Issue 64: Appearance — theme / mode / density
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Theme: 5 swatches from `THEMES` (Purple/Ocean/Forest/Sunset/Peanut).
- Mode: Auto / Dark / OLED / Light (Light only for Purple).
- Density: Roomy / Compact.
- Sidebar visibility: Visible / Hidden / Auto-hide.

### Issue 65: Library — refresh / resync
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- "Refresh library" button → triggers `core.refresh_library()` (full delta sync).
- "Clear cache" → wipes local SQLite + artwork cache.
- Progress bar + last-sync timestamp.

### Issue 66: Keyboard shortcuts editor
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** L

- Table with action / default / custom binding.
- Click a binding → "Press a shortcut…" capture state. Clash detection highlights conflicts in `danger`.
- Reset-to-default per row.

### Issue 67: Notifications
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** S

- Toggles: "Show notification on track change", "Sound", "Show in menubar while playing".
- Uses `UNUserNotificationCenter`.

### Issue 68: Advanced — developer / debug
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** S

- Show logs (opens a log viewer modal tailing `~/Library/Logs/Jellify/*.log`).
- Clear cookies, reset settings, rebuild database.
- "Enable experimental features" toggle.

### Issue 69: About panel
**Labels:** `area:macos`, `kind:polish`, `priority:p2`
**Effort:** S

- Jellyfish mark 80pt, "Jellify Desktop" italic 32pt, version + build + commit SHA, "Open source" link, licenses button (opens `NSApp.orderFrontStandardAboutPanel`), copyright line.

---

## 11. Full-screen player

**Brief.** The ritual. Cover the whole window with an ambient wash painted from two radial gradients at the theme's primary + accent (as the prototype already does). Left half: 420pt artwork with a stylized vinyl disc peeking out its right edge, spinning when playing. Right half: huge type — eyebrow "NOW PLAYING" / title 56pt / artist 20pt / album subtitle. Progress bar with live time + remaining. Transport row with a 72pt primary+accent gradient Play button. Secondary row: heart, download, queue, cast, mix, dots.

### Issue 70: Full-player entrance animation
**Labels:** `area:macos`, `area:design`, `kind:polish`, `priority:p1`
**Effort:** S

- `translateY(40px) opacity(0) → translateY(0) opacity(1)` 0.4s cubic-bezier(0.22,1,0.36,1).
- Exit reverses. Esc or minimize icon dismisses.

### Issue 71: Vinyl spin behind artwork
**Labels:** `area:macos`, `area:design`, `kind:polish`, `priority:p1`
**Effort:** S

- Conic-gradient disc (already in prototype). Spins only while playing (8s per rotation). Pauses on pause.
- 400pt disc offset `-80pt` right, `+10pt` top behind the 420pt artwork.

### Issue 72: Ambient palette from artwork
**Labels:** `area:macos`, `area:design`, `kind:feat`, `priority:p2`
**Effort:** M

- Extract 2 dominant colors from the album artwork (Vibrant.swift or `CIAreaAverage` sampling).
- Replace the theme-default primary/accent wash with the artwork's extracted colors. Blur 20pt, same radial-gradient pattern.
- Cache palette per-album in SQLite.

### Issue 73: Full player — queue drawer
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** M

- Clicking the `queue` icon slides in the queue panel from the right (340pt, same component as `QueuePanel`), overlaying the info column. Second click closes.

### Issue 74: Full player — lyrics drawer
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Clicking a "Lyrics" toggle replaces the info column with the lyrics panel, auto-scrolled and center-focused on the active line.

### Issue 75: Full player on second display
**Labels:** `area:macos`, `kind:polish`, `priority:p2`
**Effort:** M

- Menu: "Window → Move player to external display". If an external display is connected, the full player can move there while the library stays on primary.

---

## 12. Mini player

**Brief.** A detached borderless window you can tuck into a corner. Always-on-top option. Shows 60pt artwork + title + scrubber + play/pause/next. Double-click re-expands to the full app. Ideal for working-while-listening.

### Issue 76: Mini player window
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** L

- Separate SwiftUI `Window` with `.windowStyle(.hiddenTitleBar)` + `.windowResizability(.contentSize)`.
- 320×88pt. Borderless, draggable by holding and dragging the window.
- Content: 60pt artwork, title 13pt/700, artist 11pt `ink2`, 4pt scrubber, play/pause/next.

### Issue 77: Always-on-top toggle
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** S

- Right-click menu in the mini player: "Always on top", "Transparent when inactive", "Close mini player".
- Uses `NSWindow.level = .floating` for always-on-top.

### Issue 78: Mini player launcher
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** S

- Menu: "Window → Switch to Mini Player" (⌘⇧M). Full player collapses, main window minimizes to Dock, mini window opens in its last position.

---

## 13. Right panel — Now Playing

**Brief.** Keep the current structure (artwork, title, About this track, Credits) but formalize the "About" and "Credits" blocks as reusable `InfoBlock` components with 10pt uppercase `ink3` headers. Add a tasteful fact line at the bottom: "you've played this {n} times. maybe try something new?" — that italicized copy is pure Jellify character; don't lose it.

### Issue 79: Now Playing — About block
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Exactly as in `panels.jsx:44`. Rows: Album, Released, Duration, Plays, Bitrate, Size. Each row: 12pt label `ink2` + 12pt/600 value.
- Last-row border removed.

### Issue 80: Now Playing — Credits block
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Rows: Written by, Produced by, Label, Composer (if present).
- Data: `Item.People` with Role mapping.

### Issue 81: Now Playing — "fact" tagline
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- 11pt italic `ink3` centered. Variants: play count quips, "first heard {date}", "favorited {date}", "bitrate is lossless — you're hearing the master".
- Rotates every 15s.

### Issue 82: Now Playing — Go to Artist / Album / Queue link
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Three secondary links below Credits: "Go to {artist}", "Go to {album}", "Add to queue".
- 12pt `ink2` hover `ink`.

---

## 14. Right panel — Queue / Up Next

**Brief.** Two-bucket queue: the user's explicit Up Next items float on top; the auto-queue (from the currently playing album/radio/shuffle) follows. Drag between buckets to promote/demote. Header lets you Clear / Save as Playlist / Shuffle Queue.

### Issue 83: Up Next vs Auto Queue separation
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Now Playing card at top (existing).
- Section 1 header: "UP NEXT" (what the user added manually). If empty: caption "Right-click any track → Add to queue".
- Section 2 header: "PLAYING FROM {source}" (the queue inherited from the current album / radio / playlist).
- Drag from Auto → Up Next promotes; drag Up Next → Auto demotes (or just drops back into source order).

### Issue 84: Drag reorder within queue
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Standard SwiftUI `.onMove`. Ghost preview under cursor. 14pt `grip` icon on hover.

### Issue 85: Queue actions — Clear / Save / Shuffle
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Header right side: 3 icon buttons.
- Clear → confirm sheet "Clear 6 items from queue?". Save → modal asking for playlist name. Shuffle → in-place queue shuffle (keeps Now Playing first).

### Issue 86: Queue — remove single item + context menu
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Hover row → reveal `close` icon to remove. Right-click → "Play now", "Move to top", "Remove", "Go to track".

### Issue 87: Queue — history fold
**Labels:** `area:macos`, `kind:polish`, `priority:p2`
**Effort:** S

- Disclosure "Previously Played" above the Now Playing card, collapsed by default. Up to 20 recent tracks.
- Click a row → plays it + moves current to "Up Next".

---

## 15. Right panel — Lyrics

**Brief.** Already exists as a static block. Needs LRC sync so the active line highlights in real time, auto-scrolls to stay centered, and tap-to-seek jumps playback to that timestamp. Keep the typography — active line 20pt/800 ink, inactive 15pt/600 ink3, 6pt vertical padding per line.

### Issue 88: Parse LRC / timed lyrics
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Pull from `JellyfinClient.track_lyrics(track_id)` (add to core; wraps `/Audio/{id}/Lyrics`). Jellyfin returns timestamped lyrics via the LRC plugin.
- Fallback: unstamped lyrics → just render with no syncing.

### Issue 89: Auto-scroll + active line highlight
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Tick each 200ms against `playerProgress`. Active line = last line whose timestamp ≤ current time.
- `ScrollViewReader.scrollTo` with `.center` anchor. Smooth animation 300ms ease.

### Issue 90: Tap-to-seek lyric line
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Click a line → seek to its timestamp via `core.seek(timestamp)`.
- Hover highlights the line `rowHover`.

### Issue 91: Lyrics not available state
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- Empty: "No lyrics for this track. Try the Jellyfin LRC plugin." + link.
- On-server fetch failure: "Couldn't load lyrics. Retry."

---

## 16. Onboarding / First launch

**Brief.** 3 steps: Welcome, Connect server, First sync. No cloying illustrations — keep the editorial feel. Step indicator is a minimal 3-dot row, 8pt accent filled for active.

### Issue 92: Welcome step
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Jellyfish mark 120pt, "Welcome to Jellify" italic 48pt, "Your music, your server, your rules." 16pt `ink2`.
- "Get started" primary button, "I already have an account →" link.

### Issue 93: Connect server step
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Same fields as Login screen, but with a wider helper text: "Your Jellyfin URL is the address you use to reach it from a browser."
- "Skip, explore offline" secondary link if they want to poke around.

### Issue 94: First sync step + progress
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** M

- Title "Loading your library". Animated progress: artists → albums → tracks → artwork.
- Live counter: "4,208 tracks · 312 artists · 586 albums".
- "Continue to Home" button activates when sync crosses 80%.

---

## 17. Empty states

**Brief.** Soft, useful, often a touch wry. Don't over-illustrate — a single amethyst-tinted glyph + 18pt/700 headline + 13pt/500 helper + a CTA.

### Issue 95: No library content (global)
**Labels:** `area:macos`, `kind:polish`, `priority:p0`
**Effort:** S

- "Your library is empty." + "Add music to your Jellyfin server — Jellify will sync it automatically." + `Open Jellyfin dashboard` button.

### Issue 96: No search results
**Labels:** `area:macos`, `kind:polish`, `priority:p0`
**Effort:** S

- "No results for '{query}'." + "Try a different spelling, or search your Jellyfin server directly." + `Search Jellyfin` button.

### Issue 97: Empty queue
**Labels:** `area:macos`, `kind:polish`, `priority:p0`
**Effort:** S

- Queue panel with no Now Playing: "Nothing in your queue. Right-click any track to queue it up, or hit Shuffle All on Home."

### Issue 98: Empty favorites
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- "No favorites yet." + "Tap the heart on any track, album, or artist to save it here." + heart glyph.

### Issue 99: Empty downloads
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- "Nothing offline yet." + "Right-click any album and choose Download to keep it on this Mac." + download glyph.

### Issue 100: Empty playlist
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- "Empty playlist." + "Drop tracks in, or click Add tracks to browse your library." + `+` glyph.

---

## 18. Error states

**Brief.** Banner-based. Top-of-content `danger` bar with a 14pt/700 message + one action. Avoid modal blocking unless data integrity is threatened.

### Issue 101: Network offline banner
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Banner at the top of the content column: `danger` bg 10% tint, `danger` left border 3pt. Message: "You're offline. Playing from downloaded tracks only." `Retry` action.

### Issue 102: Server unreachable
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Banner: "Can't reach jellyfin.home.arpa." `Check settings` + `Retry`.
- If offline > 60s, swap Library to a "Downloaded" filter automatically.

### Issue 103: Stream failed
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Player bar flashes `danger` 10% tint + toast: "{Track title} couldn't play. Skipping." with `Retry` + `Go to track`.

### Issue 104: Auth expired
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Modal: "Your session expired. Sign in again." — only fires once per session; inline credentials, re-auth in place without losing state.

### Issue 105: Unsupported codec
**Labels:** `area:macos`, `kind:polish`, `priority:p2`
**Effort:** S

- If the track's codec isn't playable locally and transcoding isn't allowed: row marker `warning` triangle + tooltip "Transcoding required. Enable in Preferences → Playback."

---

## 19. Command palette (⌘K)

**Brief.** Spotlight-style floating palette centered at 20% from top. 640pt wide, 14pt radius, `bgAlt` with `borderStrong` outline. Single search field + a fuzzy-matched list below grouped by type. Keyboard-first: ↑↓ to navigate, Enter to execute, ⌘Enter to open in new tab/window.

### Issue 106: Command palette shell + shortcut
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- ⌘K bound globally (even from other tabs). Overlay the app, dim background to `scrim-76`.
- Input: 18pt Figtree, autofocus, debounced 80ms.

### Issue 107: Palette — library search integration
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Searches artists / albums / tracks / playlists via the same `JellyfinClient.search` backend.
- Each result row: icon (type-specific) + primary text + secondary line + right-side action hint ("↩ Play").

### Issue 108: Palette — actions (verbs, not nouns)
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Pre-defined action list: "Play", "Play next", "Add to queue", "Go to library", "Go to Home", "Go to Discover", "Open Preferences", "Toggle shuffle", "Toggle repeat", "Clear queue", "Download current album", "Switch theme → Ocean", etc.
- Actions rank above library results when the query starts with a verb.

### Issue 109: Palette — recent + pinned
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- Empty-query state: last 5 executed commands + 5 pinned items (user can right-click pin).

### Issue 110: Palette — keyboard-only UX
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- ↑↓ navigate, Enter execute, ⌘Enter new window, Esc closes, ⌘1–5 switches group filters.
- All hints shown in a 10pt `ink3` footer strip.

---

## 20. Context menus

**Brief.** Native `NSMenu` styled with our tokens where possible. Comprehensive, type-aware. Keyboard shortcuts shown on the right of each item.

### Issue 111: Track context menu
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** M

- Play (↩), Play next, Add to queue, Add to playlist…, ―, Favorite / Unfavorite, Download / Remove download, ―, Go to album, Go to artist, Start radio from this song, ―, Share link, Show in Jellyfin, ―, Info (⌘I).

### Issue 112: Album context menu
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Play, Shuffle, Play next, Add to queue, ―, Favorite, Download, Add all to playlist…, ―, Go to artist, Start album radio, ―, Share link, Show in Jellyfin.

### Issue 113: Artist context menu
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Play all, Shuffle all, Play top tracks, ―, Follow / Unfollow, Start artist radio, ―, Show similar, Share, Show in Jellyfin.

### Issue 114: Playlist context menu
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Play, Shuffle, Add to queue, ―, Rename, Duplicate, Edit rules… (smart only), ―, Share, Export (M3U / JSON), ―, Delete (with confirm).

### Issue 115: Genre context menu
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Browse genre, Start genre radio, Shuffle genre, ―, Pin to Home.

### Issue 116: Multiselect track context menu
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Same as single-track but omits Go-to actions; adds "Remove from playlist" when scoped to a playlist.

---

## 21. Global UI — cross-screen

**Brief.** Catch-all for the shared chrome: topbar, sidebar, player bar, breadcrumbs, window chrome.

### Issue 117: Breadcrumbs in top bar
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Already in prototype. Each segment clickable to jump up. Last segment is `ink`/600; rest `ink2`/500.
- "Jellify › Library › Albums › The Deep End" pattern.
- S from chat explicitly liked this — keep it.

### Issue 118: Sidebar playlist drag-reorder + collapse
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** M

- Drag playlists to reorder within the sidebar. Commits to MMKV.
- "Playlists" section header chevron collapses/expands.

### Issue 119: Sidebar auto-hide + resize
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** M

- Drag the sidebar's right edge to resize 200–320pt.
- Toggleable "Auto-hide" in Preferences.

### Issue 120: Player bar — active track progress + art peek
**Labels:** `area:macos`, `kind:polish`, `priority:p1`
**Effort:** S

- On hover of the player-bar artwork, scale 1.06 with shadow `0 10px 24px accent44`. Click opens Full Player.

### Issue 121: Menubar "Now Playing" extra
**Labels:** `area:macos`, `kind:feat`, `priority:p2`
**Effort:** M

- `MenuBarExtra` (SwiftUI): artwork + title + transport. Click → focuses main window. Shows even when app is hidden.

### Issue 122: Global keyboard shortcuts defaults
**Labels:** `area:macos`, `kind:feat`, `priority:p0`
**Effort:** S

- Space → play/pause, ←/→ with modifier → prev/next, ⌘F / ⌘K → search / palette, ⌘, → preferences, ⌘L → library, ⌘1–5 → nav, ⌘↑/↓ → volume, ⌘⇧M → mini player.

### Issue 123: Dock badge + badge count
**Labels:** `area:macos`, `kind:polish`, `priority:p2`
**Effort:** S

- Optional: show "▶" as a dock tile badge while playing. Configurable in Preferences → Notifications.

### Issue 124: Window restoration
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- Restore last size/position + screen route + right-panel mode on relaunch. Mirror the prototype's localStorage persistence in `UserDefaults`.

### Issue 125: Drag-and-drop reordering (queue + playlists)
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** S

- SwiftUI `.draggable` + `.dropDestination`. Works across queue, playlist, and sidebar zones where reordering is meaningful.

### Issue 126: Haptics on scrub
**Labels:** `area:macos`, `kind:polish`, `priority:p2`
**Effort:** S

- Trackpad haptic tick at chapter boundaries / beat markers on scrub. Core Audio provides the markers; `NSHapticFeedbackManager` triggers.

### Issue 127: AirPlay / Cast target picker
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** M

- The `cast` icon in player bar opens a popover listing AirPlay receivers + Jellyfin remote clients (if Jellyfin SyncPlay is on).

---

## 22. Mini-widgets (deferred + notable)

### Issue 128: Instant Mix modal
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** M

- From Home / Discover CTA. Modal: seed picker (track / artist / playlist / genre) + "Generate".
- Streams from `core.instant_mix(seed)`; plays immediately; queue header shows "Instant Mix · {seed}".

### Issue 129: Lockscreen / Now Playing integration
**Labels:** `area:macos`, `kind:feat`, `priority:p1`
**Effort:** M

- `MPNowPlayingInfoCenter` wiring so the macOS media keys, control center, and lockscreen all reflect Jellify state + artwork.

### Issue 130: Crossfade preview in Preferences
**Labels:** `area:macos`, `kind:polish`, `priority:p2`
**Effort:** S

- In Preferences → Playback → Crossfade slider, a small "Preview" button plays a 6-second demo swap with the current crossfade value.

---

## Design system — checklist reminders

- **Never** reintroduce a gradient wash behind the Album hero (explicit user feedback).
- Breadcrumbs stay (explicit user praise).
- All type: Figtree, no semicolons in the code, tabs for indentation in Swift.
- All accents that need "click-me" energy: `accent #CC2F71` for play pills + primary; `primary #887BFF` for secondary buttons + artist circles.
- Use `teal #57E9C9` exclusively for status (connected, downloaded, synced).
- Equalizer animation stays for the currently-playing row — it's part of Jellify's personality.
- Italic 900 h1 is the Jellify voice — use it on Home, Album, Artist, Playlist, Full Player, Onboarding. Never on Library (keep that archivist).
- The "fact-line" tagline in Now Playing ("you've played this 142 times. maybe try something new?") is character the brand should retain.
